#include<bits/stdc++.h>
#define ll long long

using namespace std;

int main()
{
    ll t;
    cin >> t;
    vector<ll>v;
    for (ll k=0; k<t; k++){
        ll n;
        cin >> n;
        ll b = sqrt(n);
        v.push_back(pow(b+1, 2) - n;)
    }
    for (auto x:v){cout << x << "\n";}
    return 0;
}
