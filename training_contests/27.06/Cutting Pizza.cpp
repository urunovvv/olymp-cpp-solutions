#include <bits/stdc++.h>
using namespace std;
#define ld long double

const ld eps = 1e-9;
const ld pi = 3.14159265358979323846;

int main(){
    int n, k;
    cin >> n >> k;
    vector<int> r(n);
    for (int &i : r) cin >> i;
    ld l = 0, r = 1e9;
    while (r - l > eps){
        
    }
}