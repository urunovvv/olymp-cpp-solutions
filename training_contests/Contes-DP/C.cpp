/*Задача о рюкзаке — это классическая задача оптимизации, где нужно выбрать 
предметы с максимальной суммарной стоимостью, не превышая заданную вместимость 
рюкзака. В вашем случае, у вас есть `n` предметов, каждый с весом `w[i]` и 
стоимостью `v[i]`.  Вам нужно найти набор предметов, которые поместятся в 
рюкзак вместимости `W` и дадут наибольшую суммарную стоимость. Важно также 
вернуть номера выбранных предметов в порядке возрастания и лексикографически 
максимальный, если таких вариантов несколько.Чтобы решить эту задачу, можно 
использовать динамическое программирование. Создадим таблицу `dp[i][w]`, где 
`dp[i][w]` представляет максимальную стоимость, которую можно получить, используя 
первые `i` предметов и рюкзак вместимости `w`.Рекуррентное соотношение будет 
следующим:*   Если `w[i] > w`, то `dp[i][w] = dp[i-1][w]` (предмет `i` слишком 
тяжелый, чтобы поместиться в рюкзак).*   Иначе, `dp[i][w] = max(dp[i-1][w], 
dp[i-1][w - w[i]] + v[i])` (мы либо не берем предмет `i`, либо берем его).После 
заполнения таблицы `dp`, `dp[n][W]` будет содержать максимальную суммарную 
стоимость. Чтобы найти выбранные предметы, нужно будет пройтись по таблице `dp` 
в обратном порядке, начиная с `dp[n][W]`, и определить, какие предметы были 
включены в оптимальное решение. Это можно сделать, сравнивая `dp[i][w]` с 
`dp[i-1][w]`. Если они равны, значит, предмет `i` не был включен. В противном 
случае, предмет `i` был включен, и мы переходим к `dp[i-1][w - w[i]]`.Так как 
нужно вывести лексикографически максимальный вариант, то предметы стоит 
рассматривать в обратном порядке, начиная с предмета с наибольшим номером. 
При выборе между включением и исключением предмета, нужно отдавать предпочтение 
включению предмета с большим номером.*/
#include <bits/stdc++.h>

using namespace std;
int main(){
    int n,W;
    cin >> n >> W;
    vector<int>w(n),v(n);
    fop(int &i:w) cin >> i;
    fop(int &i:v) cin >> i;
    vector<vector<int>>dp(n,vector<int>(n, 0));
    for (int i=0; i<n; i++){
        if (w[i] > W){
            continue;
        }
        else{
            
        }
    }
}