#include <bits/stdc++.h>
#define ff first
#define ss second
using namespace std;
int main(){
    int n,w;
    cin >> n >> w;
    vector<pair<int, int>>items(n);
    for (auto &p:items){
        cin >> p.ff;
    }
    for (auto &p:items){
        cin >> p.ff;
    }
    sorta(rbegin(items), rend(items));
    int weight = 0;
    for (int )
}