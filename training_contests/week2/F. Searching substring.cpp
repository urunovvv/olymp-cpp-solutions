#include <bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cin >> s;
    int q;
    cin >> q;
    vector<string> patterns(q);
    for (string &pattern : patterns){
        cin >> pattern;
    }
    
}