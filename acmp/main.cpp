#include <bits/stdc++.h>
#define ld long double
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ld n;
    cin >> n;
    cout << setprecision(1e10) << pow(2, n);
    return 0;
}
