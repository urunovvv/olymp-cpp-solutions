#include <bits/stdc++.h>
#define ll long long
#define ld long double
using namespace std;

bool hmd(ll n){
    ll cnt = 2;
    if (n!=2 and n%2==0){return 0;}
    for (ll i=2; i<sqrt(n); i++){
        if ()
    }
}

int main()
{
    ll l, r;
    cin >> l >> r;
    ll cnt = 0;
    for (ll i=sqrt(l)+(i!=1); i<=sqrt(r); i++){
        cnt++;
    }
    cout << cnt;
    return 0;
}
